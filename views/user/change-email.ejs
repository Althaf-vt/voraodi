<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Change Email</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <link rel="stylesheet" href="/user/changeEmailStyle.css">
</head>
<body>
    <%- include('../../views/partials/user/header') %>

    <div class="container">
        <div class="header">
            <div class="profile-section">
                <div class="profile-image-wrapper">
                    <% if (userData && userData.image) { %>
                        <img src="<%= userData.image %>" alt="User Profile Image" class="profile-image">
                    <% } else { %>
                        <div class="profile-icon">👤</div>
                    <% } %>
                </div>
                <div class="profile-text">
                    <h1>CHANGE EMAIL <span><%= userData.name %></span></h1>
                    <p>Update your account email address</p>
                </div>
            </div>
        </div>
        
        <div class="email-card">
            <div class="icon-container">
                <i class="card-icon" data-lucide="mail"></i>
            </div>
            <h3>Change Your Email</h3>
            
            <form class="email-form" action="/change-email" method="POST">
                <div class="form-group">
                    <label for="current-email">Current Email</label>
                    <input type="email" id="current-email" name="email" value="<%= userData.email %>" readonly>
                </div>
                
                <% if (typeof message !== 'undefined') { %>
                    <div id="validationMessage" class="validation-message show"><%= message %></div>
                    <script>
                        setTimeout(() => {
                            const msg = document.getElementById('validationMessage');
                            if (msg) msg.classList.remove('show');
                        }, 3000);
                    </script>
                <% } %>
                
                <div class="form-actions">
                    <button type="submit" class="action-button">Verify Email</button>
                    <a href="/userProfile" class="action-button cancel">Cancel</a>
                </div>
            </form>
        </div>
    </div>

    <script>
        lucide.createIcons();
    </script>
</body>
</html>