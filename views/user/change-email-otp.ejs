<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OTP Verification</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <link rel="stylesheet" href="/user/changeEmailStyle.css">
</head>
<body>
    <%- include('../../views/partials/user/header') %>

    <div class="container">
        <div class="header">
            <div class="profile-section">
                <div class="profile-image-wrapper">
                    <% if (userData && userData.image) { %>
                        <img src="<%= userData.image %>" alt="User Profile Image" class="profile-image">
                    <% } else { %>
                        <div class="profile-icon">👤</div>
                    <% } %>
                </div>
                <div class="profile-text">
                    <h1>OTP VERIFICATION <span><%= userData.name %></span></h1>
                    <p>Verify your email change request</p>
                </div>
            </div>
        </div>
        
        <div class="email-card card-security">
            <div class="icon-container">
                <i class="card-icon" data-lucide="shield"></i>
            </div>
            <h3>Enter Verification Code</h3>
            <p>We've sent a 6-digit code to your email</p>
            
            <form class="email-form" method="POST" action="/verify-email-otp">
                <div class="form-group">
                    <label for="otp">OTP Code</label>
                    <input type="text" id="otp" name="otp" placeholder="Enter 6-digit OTP" maxlength="6" required>
                </div>
                
                <% if (typeof message !== 'undefined') { %>
                    <div id="validationMessage" class="validation-message show"><%= message %></div>
                    <script>
                        setTimeout(() => {
                            const msg = document.getElementById('validationMessage');
                            if (msg) msg.classList.remove('show');
                        }, 3000);
                    </script>
                <% } %>
                
                <div class="otp-timer">
                    <span id="timer">0:60</span>
                </div>
                
                <div class="form-actions">
                    <button type="submit" class="action-button">Verify OTP</button>
                    <button type="button" class="action-button resend-otp" disabled>Resend OTP</button>
                </div>
            </form>
        </div>
    </div>

    <script>
        lucide.createIcons();
        
        // OTP Timer
        let timeLeft = 60;
        const timerElement = document.getElementById('timer');
        const resendButton = document.querySelector('.resend-otp');

        function startTimer() {
            const timer = setInterval(() => {
                timeLeft--;
                const minutes = Math.floor(timeLeft / 60);
                const seconds = timeLeft % 60;
                timerElement.textContent = `${minutes}:${seconds < 10 ? '0' : ''}${seconds}`;
                if (timeLeft <= 0) {
                    clearInterval(timer);
                    resendButton.disabled = false;
                    resendButton.classList.remove('disabled');
                    timerElement.textContent = 'Expired';
                }
            }, 1000);
        }

        startTimer();

        resendButton.addEventListener('click', function() {
            if (!resendButton.disabled) {
                timeLeft = 60;
                resendButton.disabled = true;
                resendButton.classList.add('disabled');
                startTimer();
            }
        });
    </script>
</body>
</html>